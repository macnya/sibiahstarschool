
// This page and its functionality have been removed as per user request.
// You can delete this file and the /teacher-portal directory.
import { PageHeader } from '@/components/shared/page-header';

export default function TeacherPortalRemovedPage() {
  return (
    <div>
      <PageHeader
        title="Teacher Portal"
        subtitle="This portal has been removed."
      />
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-16 md:py-20 text-center">
        <p>The Teacher Portal functionality is no longer available.</p>
      </div>
    </div>
  );
}
